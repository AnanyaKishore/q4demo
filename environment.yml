name: assignment1_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - pip
  - pip:
    - pandas
    - scikit-learn
    - mlflow
    - dvc[s3]
    - awscli
    - jupyter
    - ipywidgets
